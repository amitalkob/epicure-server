{
	"apps": [
		{
			"name": "epicure-server",
			"script": "./dist/server.js",
			"env": {
				"NODE_ENV": "development"
			}
		}
	],

	"deploy": {
		"staging": {
			"key": "/Users/amitalkobi/GitHub/amit-key.pem",
			"user": "ubuntu",
			"host": ["ec2-3-131-97-117.us-east-2.compute.amazonaws.com"],
			"ref": "origin/develop",
			"repo": "git@github.com:amitalkob/epicure-server.git",
			"path": "/home/ubuntu/Server",
			"ssh_options": "StrictHostKeyChecking=no",
			"post-deploy": "mkdir -p logs && npm i && pm2 reload ecosystem.json --env staging",
			"pre-deploy-local": "echo 'Deploying code to servers'",
			"env": {
				"NODE_ENV": "staging"
			}
		}
	}
}

